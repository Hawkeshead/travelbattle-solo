{
  "name": "travelbattle-field-command",
  "version": "1.0.0",
  "private": true,
  "description": "A Napoleonic tabletop battle, adapted for the phone.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -o",
    "test": "node --test \"test/*.test.js\"",
    "check": "node tools/check-assets.mjs && node tools/check-module-boundaries.mjs && npm test",
    "smoke": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.5",
    "http-server": "^14.1.1"
  }
}
