{
  "name": "travelbattle-field-command",
  "version": "1.0.0",
  "private": true,
  "description": "A Napoleonic tabletop battle, adapted for the phone.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -o",
    "test": "node --test \"test/*.test.js\"",
    "check": "node tools/check-assets.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "http-server": "^14.1.1"
  }
}
